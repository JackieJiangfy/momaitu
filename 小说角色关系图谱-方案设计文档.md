# 小说角色关系图谱系统 — 方案设计文档

> 版本：v1.0 | 日期：2026-08-06 | 状态：设计评审

---

## 一、项目概述

### 1.1 项目定位

构建一个**通用的网页端小说角色关系图谱系统**，用户注册后可创建多部小说，为每部小说管理角色及其关系，系统自动生成力导向关系图谱进行可视化展示。

### 1.2 核心价值

- **关系可视化**：将小说中复杂的人物关系以力导向图形式直观呈现
- **通用平台**：不绑定特定小说，任何用户可管理任意小说作品
- **标准化建模**：内置 5 大类 25 种关系类型，支持自定义扩展
- **交互丰富**：拖拽、缩放、悬浮高亮、搜索过滤、导出图片

### 1.3 参考对标

Obsidian Graph View（知识笔记关系图），但专为小说角色关系场景定制。

---

## 二、技术选型

| 层 | 技术 | 选型理由 |
|---|------|---------|
| 前端框架 | **Vue 3** + Vite + TypeScript | 响应式、生态成熟、开发者熟悉 |
| UI 组件库 | **Arco Design** | 表格/表单/弹窗/卡片开箱即用 |
| 图谱引擎 | **AntV G6 v4** | 蚂蚁集团出品，内置力导向布局，有 Vue3 封装 |
| 后端框架 | **Spring Boot 2.x** + Maven | 成熟稳定、生态完善 |
| ORM | **MyBatis Plus** | 代码生成器 + Lambda 查询，效率高 |
| 鉴权 | **Sa-Token** | 轻量、支持注解鉴权、前后端分离友好 |
| 数据库 | **MySQL 8.0** | 新建独立库 `novel_graph` |

### 选型对比

| 方案 | 渲染方式 | 性能上限 | Vue3适配 | 学习成本 | 推荐度 |
|------|---------|---------|----------|---------|--------|
| **AntV G6** | Canvas/SVG | ~10k 节点 | 优秀 | 中 | ★★★★★ |
| D3.js | SVG/Canvas | 灵活 | 需手动封装 | 高 | ★★★ |
| Cytoscape.js | Canvas | ~5k 节点 | 一般 | 中 | ★★★ |
| Sigma.js | WebGL | ~50k 节点 | 需手动封装 | 高 | ★★ |
| vis-network | Canvas | ~3k 节点 | 一般 | 低 | ★★ |

---

## 三、系统架构

### 3.1 整体架构（前后端分离）

```
┌─────────────────────────────────────────────┐
│                  前端 (Vue3)                 │
│  ┌───────┐ ┌───────┐ ┌───────┐ ┌─────────┐ │
│  │登录注册│ │小说管理│ │角色管理│ │图谱可视化│ │
│  └───────┘ └───────┘ └───┬───┘ └────┬────┘ │
│                    ┌──────┴──────┐   │       │
│                    │  角色/关系CRUD │  │G6渲染│ │
│                    └──────┬──────┘   │       │
├───────────────────────────┼──────────┼───────┤
│               HTTP / JSON  │          │       │
├───────────────────────────┼──────────┼───────┤
│                  后端 (Spring Boot)           │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐ │
│  │ Auth模块 │ │ 业务模块  │ │  图谱数据模块 │ │
│  └──────────┘ └──────────┘ └──────────────┘ │
│          ┌──────────────────┐                │
│          │  MySQL (novel_graph)               │
│          └──────────────────┘                │
└─────────────────────────────────────────────┘
```

### 3.2 图谱引擎五层架构

```
┌─────────────────────────────────────────────┐
│  UI 层      │ 工具栏 │ 搜索栏 │ 过滤器 │ 图例 │
├─────────────────────────────────────────────┤
│  交互层     │ 缩放平移 │ 节点拖拽 │ 悬浮高亮 │ 点击导航 │
├─────────────────────────────────────────────┤
│  渲染层     │ Canvas │ WebGL │ SVG渲染 │ 动画引擎 │
├─────────────────────────────────────────────┤
│  布局引擎   │ 斥力模型 │ 弹簧模型 │ 中心引力 │ 碰撞检测 │
├─────────────────────────────────────────────┤
│  数据层     │ 节点模型 │ 边模型 │ 数据加载 │ API接口 │
└─────────────────────────────────────────────┘
```

### 3.3 数据流

```
用户登录 → 获取Token → 查看我的小说列表
                       ↓
              选择小说 → 管理角色(CRUD)
                       ↓
              建立关系(选两个角色 + 选关系类型)
                       ↓
              进入图谱页 → API返回{ nodes, edges }
                       ↓
              G6渲染力导向图 → 用户交互(拖拽/缩放/高亮/搜索)
```

---

## 四、数据库设计

### 4.1 数据库：novel_graph（MySQL 8.0）

### 4.2 ER 关系

```
sys_user (1) ────── (N) novel (1) ────── (N) novel_character
                          │                         │
                          │                         │ (N)
                          │                    novel_relationship (N)
                          │                         │
                          └── (N) novel_rel_type_config
```

### 4.3 表结构 DDL

```sql
-- ==================== 1. 用户表 ====================
CREATE TABLE sys_user (
  id          VARCHAR(32)  PRIMARY KEY COMMENT '主键，UUID',
  username    VARCHAR(64)  NOT NULL UNIQUE COMMENT '用户名',
  password    VARCHAR(128) NOT NULL COMMENT '加密密码',
  nickname    VARCHAR(64)  COMMENT '昵称',
  avatar      VARCHAR(512) COMMENT '头像URL',
  status      VARCHAR(16)  DEFAULT 'ACTIVE' COMMENT 'ACTIVE/DISABLED',
  created_at  DATETIME     DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- ==================== 2. 小说表 ====================
CREATE TABLE novel (
  id          VARCHAR(32)  PRIMARY KEY COMMENT '主键，UUID',
  user_id     VARCHAR(32)  NOT NULL COMMENT '所属用户ID',
  title       VARCHAR(128) NOT NULL COMMENT '小说名称',
  author      VARCHAR(64)  COMMENT '作者',
  description TEXT         COMMENT '简介',
  cover_url   VARCHAR(512) COMMENT '封面图片URL',
  status      VARCHAR(16)  DEFAULT 'ACTIVE' COMMENT 'ACTIVE/ARCHIVED',
  created_at  DATETIME     DEFAULT CURRENT_TIMESTAMP,
  updated_at  DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='小说表';

-- ==================== 3. 角色表 ====================
CREATE TABLE novel_character (
  id            VARCHAR(32)  PRIMARY KEY COMMENT '主键，UUID',
  novel_id      VARCHAR(32)  NOT NULL COMMENT '所属小说ID',
  name          VARCHAR(64)  NOT NULL COMMENT '角色名称',
  alias         VARCHAR(256) COMMENT '别名/称号，逗号分隔',
  faction       VARCHAR(64)  COMMENT '所属势力/门派/阵营',
  role_type     VARCHAR(32)  COMMENT '定位：主角/配角/反派/路人',
  species       VARCHAR(32)  COMMENT '种族：人/仙/魔/妖/其他',
  avatar_url    VARCHAR(512) COMMENT '角色头像URL',
  description   TEXT         COMMENT '角色简介',
  first_chapter VARCHAR(32)  COMMENT '首次出场章节',
  power_level   VARCHAR(32)  COMMENT '战力等级（自定义）',
  sort_order    INT          DEFAULT 0 COMMENT '排序权重',
  created_at    DATETIME     DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_novel_id (novel_id),
  INDEX idx_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';

-- ==================== 4. 关系表 ====================
CREATE TABLE novel_relationship (
  id            VARCHAR(32)  PRIMARY KEY COMMENT '主键，UUID',
  novel_id      VARCHAR(32)  NOT NULL COMMENT '所属小说ID',
  source_id     VARCHAR(32)  NOT NULL COMMENT '源角色ID',
  target_id     VARCHAR(32)  NOT NULL COMMENT '目标角色ID',
  rel_type      VARCHAR(32)  NOT NULL COMMENT '关系类型：父子/师徒/恋人/仇敌...',
  category      VARCHAR(16)  NOT NULL COMMENT '关系性质：positive/neutral/negative',
  directed      TINYINT      DEFAULT 0 COMMENT '是否单向：0双向 1单向（如暗恋）',
  intensity     TINYINT      DEFAULT 5 COMMENT '关系强度：1-10',
  description   VARCHAR(256) COMMENT '关系说明（如"第三百章结为道侣"）',
  start_chapter VARCHAR(32)  COMMENT '关系形成章节',
  end_chapter   VARCHAR(32)  COMMENT '关系结束章节',
  created_at    DATETIME     DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_novel_id (novel_id),
  INDEX idx_source (source_id),
  INDEX idx_target (target_id),
  UNIQUE KEY uk_relation (novel_id, source_id, target_id, rel_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='关系表';

-- ==================== 5. 关系类型配置表 ====================
CREATE TABLE novel_rel_type_config (
  id        VARCHAR(32)  PRIMARY KEY COMMENT '主键，UUID',
  novel_id  VARCHAR(32)  NOT NULL COMMENT '所属小说ID',
  type_name VARCHAR(32)  NOT NULL COMMENT '关系类型名称',
  category  VARCHAR(16)  NOT NULL COMMENT '关系性质：positive/neutral/negative',
  icon      VARCHAR(32)  COMMENT '图标标识（可选）',
  sort_order INT         DEFAULT 0 COMMENT '排序权重',
  INDEX idx_novel_id (novel_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='关系类型配置';
```

### 4.4 预置关系类型数据

新建小说时，自动复制以下 25 种预设关系类型：

| 大类 | 关系性质 | 关系类型 |
|------|---------|---------|
| 血缘关系 | positive | 父子、母子、兄妹、祖孙、姻亲 |
| 情感关系 | positive | 恋人、夫妻、暗恋、知己、前任 |
| 社交关系 | neutral  | 朋友、师徒、同门、盟友、从属 |
| 敌对关系 | negative | 仇敌、宿敌、对手、叛徒、对立 |
| 特殊关系 | neutral  | 主仆、转世、契约、宿命、分身 |

---

## 五、API 接口设计

### 5.1 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 5.2 接口清单

#### 认证模块 (`/api/auth`)

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/api/auth/register` | 用户注册 | 否 |
| POST | `/api/auth/login` | 登录，返回 Token | 否 |
| GET | `/api/auth/info` | 获取当前用户信息 | 是 |
| PUT | `/api/auth/password` | 修改密码 | 是 |
| PUT | `/api/auth/profile` | 修改个人信息 | 是 |

#### 小说模块 (`/api/novels`)

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/api/novels` | 我的小说列表（支持搜索） | 是 |
| POST | `/api/novels` | 创建小说 | 是 |
| GET | `/api/novels/:id` | 小说详情 | 是 |
| PUT | `/api/novels/:id` | 编辑小说信息 | 是 |
| DELETE | `/api/novels/:id` | 删除小说（软删除→归档） | 是 |

#### 角色模块 (`/api/novel/:novelId/characters`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/novel/:nid/characters` | 角色列表（支持搜索/分页） |
| POST | `/api/novel/:nid/characters` | 新增角色 |
| GET | `/api/novel/:nid/characters/:cid` | 角色详情 |
| PUT | `/api/novel/:nid/characters/:cid` | 编辑角色 |
| DELETE | `/api/novel/:nid/characters/:cid` | 删除角色 |
| POST | `/api/novel/:nid/characters/batch` | 批量导入角色 |

#### 关系模块 (`/api/novel/:novelId/relationships`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/novel/:nid/relationships` | 关系列表 |
| POST | `/api/novel/:nid/relationships` | 添加关系 |
| PUT | `/api/novel/:nid/relationships/:rid` | 编辑关系 |
| DELETE | `/api/novel/:nid/relationships/:rid` | 删除关系 |
| GET | `/api/novel/:nid/rel-types` | 关系类型列表 |
| POST | `/api/novel/:nid/rel-types` | 自定义关系类型 |

#### 图谱模块 (`/api/novel/:novelId/graph`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/novel/:nid/graph` | 获取图谱数据 |

**图谱数据响应格式**：

```json
{
  "code": 200,
  "data": {
    "novelTitle": "诛仙",
    "nodes": [
      {
        "id": "c001",
        "name": "张小凡",
        "faction": "青云门",
        "roleType": "主角",
        "species": "人",
        "avatarUrl": null,
        "relationCount": 8
      }
    ],
    "edges": [
      {
        "id": "r001",
        "source": "c001",
        "target": "c002",
        "relType": "师徒",
        "category": "positive",
        "directed": false,
        "intensity": 8
      }
    ]
  }
}
```

---

## 六、前端页面设计

### 6.1 路由规划

```
/login                    登录/注册页
/novels                   我的小说列表（卡片视图）
/novel/:id/characters     角色管理（表格 + 表单弹窗）
/novel/:id/relationships  关系管理（选角色 + 配置关系）
/novel/:id/graph          图谱可视化（核心页面）
/profile                  个人中心
```

### 6.2 图谱页布局

```
┌──────────┬─────────────────────────────────────────┐
│  侧边栏   │              导航栏                       │
│ (280px)  │  ← 返回小说列表 | 小说名称 | 导出 | 设置  │
│          ├─────────────────────────────────────────┤
│  🔍搜索  │                                         │
│ ┌──────┐ │         力导向关系图谱                     │
│ │搜索框│ │                                         │
│ └──────┘ │   🔵 田不易 ── 师徒 ── 🔵 张小凡          │
│          │    │师兄            │恋人                │
│ 势力过滤  │   🔵 宋大仁       🟢 陆雪琪               │
│ ☑ 青云门 │    │暗恋           │仇敌                 │
│ ☑ 天音寺 │   🟢 田灵儿       🔴 鬼厉                │
│ ☑ 焚香谷 │                                         │
│ ☑ 魔教   │         连线颜色说明：                    │
│          │         绿色=正面  灰色=中性  红色=敌对     │
│ 角色定位  │                                         │
│ ☑ 主角   ├─────────────────────────────────────────┤
│ ☑ 配角   │  角色详情面板（点击节点展开）              │
│ ☑ 反派   │  "张小凡 / 青云门 / 主角"                 │
│          │  关联角色：8个 | 关系数：11条              │
│ 关系类型  │  [编辑角色] [查看全部关系]                 │
│ ☑ 血缘   │                                         │
│ ☑ 情感   │                                         │
│ ☑ 社交   │                                         │
│ ☑ 敌对   │                                         │
│ ☑ 特殊   │                                         │
└──────────┴─────────────────────────────────────────┘
```

### 6.3 图谱渲染规则

| 元素 | 渲染规则 |
|------|---------|
| 节点颜色 | 按势力（faction）着色，系统自动分配颜色或用户自定义 |
| 节点大小 | 按关联数（relationCount）动态计算，关联越多越大 |
| 连线颜色 | 绿色实线=正面关系、灰色虚线=中性关系、红色实线=敌对关系 |
| 连线标签 | 悬浮时显示关系类型文字（如"师徒""恋人"） |
| 高亮行为 | 悬浮节点 → 高亮该节点及所有关联节点和边，其他节点变淡 |

### 6.4 图谱交互

| 操作 | 行为 |
|------|------|
| 滚轮 | 缩放画布 |
| 拖拽空白区 | 平移画布 |
| 拖拽节点 | 移动节点位置（松开后力导向自动调整） |
| 悬浮节点 | 高亮关联 + 显示关系标签 |
| 点击节点 | 右侧展开角色详情面板 |
| 双击节点 | 聚焦该节点，动画居中 |
| 搜索框输入 | 匹配角色名，高亮并居中定位 |
| 过滤器勾选 | 按势力/定位/关系类型过滤显示 |

---

## 七、前端数据模型

```typescript
// ========== 角色节点 ==========
interface CharacterNode {
  id: string              // 唯一标识
  name: string            // 角色名
  alias?: string[]        // 别名
  faction?: string        // 所属势力
  roleType?: string       // 主角/配角/反派/路人
  species?: string        // 种族
  avatarUrl?: string      // 头像URL
  description?: string    // 简介
  firstChapter?: string   // 首次出场
  powerLevel?: string     // 战力等级
  relationCount: number   // 关联数（用于计算节点大小）
}

// ========== 关系边 ==========
interface RelationshipEdge {
  id: string              // 关系ID
  source: string          // 源角色ID
  target: string          // 目标角色ID
  relType: string         // 关系类型
  category: 'positive' | 'neutral' | 'negative'
  directed: boolean       // 是否单向
  intensity: number       // 强度 1-10
  description?: string    // 关系描述
  startChapter?: string   // 起始章节
  endChapter?: string     // 结束章节
}

// ========== 图谱数据 ==========
interface GraphData {
  novelTitle: string
  nodes: CharacterNode[]
  edges: RelationshipEdge[]
}

// ========== 小说 ==========
interface Novel {
  id: string
  title: string
  author?: string
  description?: string
  coverUrl?: string
  status: 'ACTIVE' | 'ARCHIVED'
  characterCount: number
  relationCount: number
  createdAt: string
}

// ========== 搜索过滤条件 ==========
interface GraphFilter {
  keyword?: string         // 角色名搜索
  factions?: string[]      // 势力过滤
  roleTypes?: string[]     // 定位过滤
  relCategories?: string[] // 关系性质过滤
}
```

---

## 八、性能方案

| 节点规模 | 渲染方案 | 布局优化 | 预期帧率 |
|---------|---------|---------|---------|
| < 200 | Canvas | 暴力 O(n²) | 60fps |
| 200 ~ 2000 | Canvas | 四叉树斥力优化 | 30~60fps |
| > 2000 | WebGL | LOD 层级 + 简化物理 | 30fps |

对于小说角色图谱（通常 20~200 个角色），Canvas 渲染完全足够，无需额外优化。

---

## 九、开发计划

| 阶段 | 范围 | 主要产出 |
|------|------|---------|
| **Phase 1** | 后端基础 | 建库建表 → 用户认证 API → 小说 CRUD API → 角色 CRUD API → 关系 CRUD API |
| **Phase 2** | 前端管理 | 登录注册页 → 小说列表/创建 → 角色管理表格 → 关系编辑表单 → 前后端联调 |
| **Phase 3** | 图谱可视化 | G6 集成 → 力导向渲染 → 势力/关系着色 → 拖拽/缩放/悬浮高亮 |
| **Phase 4** | 功能完善 | 搜索过滤 → 点击详情面板 → 导出图片 → 批量导入 → 暗黑模式 |

### 项目目录结构规划

```
novel-graph/
├── backend/                        # Spring Boot 后端
│   ├── src/main/java/com/novelgraph/
│   │   ├── controller/             # 控制器
│   │   │   ├── AuthController.java
│   │   │   ├── NovelController.java
│   │   │   ├── CharacterController.java
│   │   │   ├── RelationshipController.java
│   │   │   └── GraphController.java
│   │   ├── service/                # 业务层
│   │   ├── mapper/                 # MyBatis Plus Mapper
│   │   ├── entity/                 # 实体类
│   │   ├── dto/                    # 数据传输对象
│   │   ├── config/                 # 配置（SaToken/CORS/MyBatisPlus）
│   │   └── common/                 # 统一响应/R/异常处理
│   ├── src/main/resources/
│   │   ├── application.yml
│   │   └── db/
│   │       └── schema.sql          # 建表 + 预置数据
│   └── pom.xml
│
├── frontend/                       # Vue3 前端
│   ├── src/
│   │   ├── views/
│   │   │   ├── Login.vue           # 登录注册
│   │   │   ├── NovelList.vue       # 小说列表
│   │   │   ├── CharacterManage.vue # 角色管理
│   │   │   ├── RelationManage.vue  # 关系管理
│   │   │   ├── GraphView.vue       # 图谱可视化（核心）
│   │   │   └── Profile.vue         # 个人中心
│   │   ├── components/
│   │   │   ├── GraphCanvas.vue     # G6 图谱画布组件
│   │   │   ├── GraphSidebar.vue    # 图谱侧边栏（搜索过滤）
│   │   │   ├── CharacterForm.vue   # 角色表单弹窗
│   │   │   ├── RelationForm.vue    # 关系表单弹窗
│   │   │   └── CharacterDetail.vue # 角色详情面板
│   │   ├── api/                    # 接口封装
│   │   │   ├── auth.ts
│   │   │   ├── novel.ts
│   │   │   ├── character.ts
│   │   │   ├── relationship.ts
│   │   │   └── graph.ts
│   │   ├── router/                 # 路由
│   │   ├── stores/                 # Pinia 状态管理
│   │   ├── types/                  # TypeScript 类型定义
│   │   └── utils/                  # 工具函数
│   ├── package.json
│   └── vite.config.ts
│
└── README.md
```

---

## 十、GraphView.vue 核心设计（伪代码）

```vue
<template>
  <div class="graph-page">
    <!-- 侧边栏 -->
    <GraphSidebar
      :characters="characters"
      :filter="filter"
      @search="onSearch"
      @filter-change="onFilterChange"
    />

    <!-- 图谱主区域 -->
    <div class="graph-main">
      <div class="graph-toolbar">
        <a-button @click="zoomIn">放大</a-button>
        <a-button @click="zoomOut">缩小</a-button>
        <a-button @click="fitView">适应画布</a-button>
        <a-button @click="exportImage">导出图片</a-button>
      </div>

      <!-- G6 画布 -->
      <GraphCanvas
        ref="graphRef"
        :data="graphData"
        :highlighted-node="highlightedNode"
        @node-click="onNodeClick"
        @node-hover="onNodeHover"
      />

      <!-- 角色详情面板（点击显示） -->
      <CharacterDetail
        v-if="selectedCharacter"
        :character="selectedCharacter"
        :relations="selectedRelations"
        @close="selectedCharacter = null"
      />
    </div>
  </div>
</template>

<script setup lang="ts">
// 1. 进入页面 → 调用 GET /api/novel/:nid/graph
// 2. 拿到 { nodes, edges } → 传给 GraphCanvas
// 3. G6 内部：
//    - 创建 Graph 实例，配置 gForce 布局
//    - 按 faction 映射节点颜色
//    - 按 category 映射边颜色（positive=green/neutral=gray/negative=red）
//    - 注册 node:mouseenter 事件 → 调用 graph.getNeighbors() 高亮
//    - 注册 node:click 事件 → 发射事件给父组件显示详情
// 4. 搜索/过滤 → 前端 filter 数据后 graph.changeData()
</script>
```

---

## 十一、关系连线设计规范

| 关系性质 | 连线颜色 | 线型 | 用途 |
|---------|---------|------|------|
| positive | `#639922`（绿） | 实线 | 血缘、情感、同盟 |
| neutral  | `#888780`（灰） | 虚线（4px dash, 3px gap） | 社交、同门、特殊 |
| negative | `#D85A30`（红） | 实线 | 敌对、仇敌、对立 |

---

## 十二、风险与备注

- **G6 版本锁定**：建议锁定 `@antv/g6@4.x`，v5 有 breaking changes
- **图谱页性能**：单部小说角色数通常 < 200，Canvas 渲染无压力
- **关系去重**：数据库已加 `uk_relation` 唯一约束，避免同类型重复关系
- **预置数据**：新建小说时 `NovelService.createNovel()` 自动插入 25 条关系类型配置
- **前端状态**：使用 Pinia 管理当前小说 ID + 图谱数据，避免重复请求

---

*文档结束。待评审通过后进入 Phase 1 开发。*
